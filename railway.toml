[build]
  root = "backend"

[deploy]
  startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"

[env]
  ACCESS_TOKEN_EXPIRE_MINUTES = "1440"
  CORS_ORIGINS = "[\"https://anthony-sistem.netlify.app\"]"
  SECRET_KEY = "cambia_esta_clave_en_railway"

